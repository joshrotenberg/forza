## Review: issue #527

### Summary

The change adds `RetryPolicy::new().exponential().retry_on_timeout(true)` to both the `ClaudeAdapter` (executor.rs) and `CodexAgentAdapter` (adapters.rs) builder chains. This delegates transient CLI failure retry logic to the wrapper layer, avoiding full forza stage re-runs for timeouts or process crashes.

### Checklist

- **Correctness**: Both builders receive the retry policy before `.build()`. The change matches the plan exactly — no new fields, traits, or config surface added.
- **Consistency**: Style matches surrounding code in both files. The retry call is chained inline with the builder pattern already in use.
- **Tests**: No new tests added for the retry behavior. This is acceptable because the retry logic lives inside the wrapper libraries (`claude-wrapper`, `codex-wrapper`), which are expected to have their own tests. The forza-level test suite (134 unit tests, pipeline integration tests) continues to pass unchanged.
- **Build**: `cargo build -p forza` succeeds. `cargo test --all` passes. `cargo clippy -p forza --all-targets` produces no warnings or errors.

### Issues found

| Severity | File | Line | Description |
|----------|------|------|-------------|

No issues found.

### Verdict: PASS
