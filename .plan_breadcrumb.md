## Files to modify

- `crates/forza-core/src/commands/draft_pr.sh`
- `.plan_breadcrumb.md` (remove from git tracking via `git rm --cached`)

## Approach

The root cause is that `draft_pr.sh` explicitly stages `.plan_breadcrumb.md` and `.forza/` via `git add` before committing, which overrides the `.gitignore` entries that already list `*.breadcrumb.md` and `.forza/`. Since the script already reads `.plan_breadcrumb.md` and passes its content as the PR `--body`, the file does not need to be committed at all — remove the lines that stage breadcrumb files and create the breadcrumb-only commit (lines 7–13 of the script). The push and `gh pr create` calls remain unchanged. The implement stage must also untrack the already-committed `.plan_breadcrumb.md` file from the repo via `git rm --cached .plan_breadcrumb.md` so it is no longer present on main after merge.

## Commit message

`fix(draft-pr): stop committing breadcrumb files to branch closes #282`
